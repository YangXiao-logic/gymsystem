server:
  port: 80

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/gym?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&useSSL=false
    username: root
    password: root
  redis:
    host: localhost # Redis?????
    database: 0 # Redis?????????0?
    port: 6379 # Redis???????
    password: # Redis?????????????
    jedis:
      pool:
        max-active: 8 # ????????????????????
        max-wait: -1ms # ???????????????????????
        max-idle: 8 # ???????????
        min-idle: 0 # ???????????
    timeout: 3000ms # ??????????

mybatis:
  mapper-locations:
    - classpath:mapper/*.xml
    - classpath*:com/**/mapper/*.xml

# ???redis key
redis:
  key:
    prefix:
      authCode: "portal:authCode:"
    expire:
      authCode: 120 # ???????

# ???jwt key
jwt:
  tokenHeader: Authorization #JWT??????
  secret: mySecret #JWT????????
  expiration: 604800 #JWT??????(60*60*24)
  tokenHead: Bearer  #JWT???????
